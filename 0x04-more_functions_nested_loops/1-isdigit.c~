#include "main.h"
/**
 * _isdigit - return digit c as 1
 * @c: C is 1
 * Return: (0)
 */
int _isdigit(int c);
{
if (c >= 'a' && c <= 'z')
{
return (1);
}
else if (c >= 'A' && c <= 'Z')
{
return (1);
}
else
{
return (0);
}
}
