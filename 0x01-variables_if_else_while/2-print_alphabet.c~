#include <stdio.h>
/**
 * main - main Prints the alpha in lowercase
 *
 * Return: returns to 0
 */
init main(void)
{
char letter;
for (letter = 'a'; letter <= 'z'; letter++)
putchar(letter);
putchar('\n');
return (0);
}
